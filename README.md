# Media Viewer

ローカルにある画像・動画ファイルを Web ブラウザから手軽に閲覧できるメディアビューアーです。  
`public/` 配下に置いたフォルダを一覧表示し、画像サムネイル・動画プレビューを自動生成します。パスワード認証に対応しており、Tailscale と組み合わせることで外部端末からのアクセスも可能です。

## 動作環境

- Windows 10 / 11（動作確認済み）
- [Node.js](https://nodejs.org/)（最新の LTS バージョン推奨）

## 初期設定

1. [Node.js](https://nodejs.org/) の最新 LTS バージョンをインストールします。
2. `public/` 配下に任意の名前でフォルダを作成し、その中へ表示したい画像・動画ファイルを配置します。

    ```
    public/
    ├── FOLDER_001/
    │   ├── IMAGE_001.jpe
    │   ├── IMAGE_002.jpeg
    │   ├── IMAGE_003.png
    │   ├── IMAGE_004.webp
    │   └── ...
    ├── FOLDER_002/
    │   ├── VIDEO_001.mp4
    │   ├── VIDEO_002.mov
    │   ├── VIDEO_003.gif
    │   ├── VIDEO_004.webm
    │   └── ...
    └── ...
    ```
3. **【任意】** Tailscale 経由で外部端末からアクセスできるようにする場合は、以下の手順で設定します。
    <details>
    <summary>手順を表示</summary>

    1. [Tailscale](https://tailscale.com) を本アプリを動かす OS にインストールし、アカウントへサインインします。
    2. [Tailscale 管理コンソール（DNS）](https://login.tailscale.com/admin/dns) を開き、以下の 2 つを有効化します。
        - **MagicDNS**
        - **HTTPS Certificates**
    3. 本アプリを動かす OS のターミナル（CommandPrompt、PowerShell など）で以下を実行し、Tailnet 内からの HTTPS (443) アクセスをアプリのポート `3080` へ転送します。

        ```
        tailscale serve --bg 3080
        ```
        ※ 転送を停止する場合は `tailscale serve reset` を実行します。
    4. [Tailscale 管理コンソール（Machines）](https://login.tailscale.com/admin/machines) から、対象マシンの **DNS 名** を確認します。形式は次のとおりです。

        ```
        <マシン名>.<Tailnet 識別子>.ts.net
        ```
    5. 確認した DNS 名を `config.json` の `tailnetDomain` に設定します。

        ```json
        {
          "tailnetDomain": "<マシン名>.<Tailnet 識別子>.ts.net"
        }
        ```
    </details>

## 起動方法

プロジェクトのルートにある `boot.bat` を実行してください。起動時には以下の処理が自動で実行されます。

1. Node.js 依存パッケージのインストール（初回のみ）
2. 未生成の画像サムネイル・動画プレビューの生成
3. ポート `3080` でのサーバー起動
4. アクセス先 URL を既定のブラウザで自動表示
    - Tailscale 設定なし：`http://<LAN 内の IP アドレス>:3080`
    - Tailscale 設定あり：`https://<マシン名>.<Tailnet 識別子>.ts.net`

<br>

> **パスワードを変更する場合**  
> `boot.bat` 内のコマンドは `npm start <パスワード>` の形式になっています。末尾の `password` を任意の文字列に書き換えてください。

## 利用規約

### 1. ライセンス

本アプリのソースコードは [ISC ライセンス](https://opensource.org/license/isc-license-txt) のもとで公開しています。ライセンスの範囲内であれば、自由に利用・改変・再配布していただいて構いません。  
ただし、本アプリが利用している各依存パッケージ（Express、sharp、ffmpeg-static など）については、それぞれのライセンス条項に従ってください。

### 2. 著作権

本アプリのソースコードおよび関連ドキュメントの著作権は、作者または各依存パッケージの権利者に帰属します。  
一方で、`public/` 配下に配置する画像・動画ファイル（以下「コンテンツ」と呼びます）の著作権は、配置されたユーザー本人または正当な権利者に帰属します。本アプリ側でこれらの権利を主張することはありません。  
なお、第三者の著作権・肖像権・プライバシー権などを侵害するコンテンツの配置・配信はおやめください。

### 3. 免責事項

本アプリは現状のまま（AS IS）で提供しています。作者は明示・黙示を問わず、本アプリの完全性・正確性・特定目的への適合性・継続的な動作について保証しません。  
本アプリを利用されたこと、または利用できなかったことによって生じた一切の損害について、作者は責任を負いません。ただし、作者の故意または重大な過失による場合は、この限りではありません。

### 4. ご利用にあたっての注意

- 本アプリにはフォルダ単位で ZIP ダウンロードを行う機能があります。実行時に一時データを生成するため、ディスク容量に余裕がある状態でご利用ください。
- 本アプリのパスワード認証は簡易的なものです。堅牢なセキュリティを保証するものではないため、以下の点に注意してください。
    - パスワードはコマンドライン引数として渡されるため、プロセス一覧などから参照される可能性があります
    - 認証 Cookie は最大 30 日間保持されるため、共用端末での利用はおすすめしません
    - LAN 公開時の通信は HTTP（非暗号化）で行われます
    - 機密性の高いコンテンツや、外部に流出しては困るコンテンツの取り扱いはおすすめしません

### 5. お問い合わせ

本アプリや本規約に関するご質問・ご要望・不具合のご報告は、本アプリを公開している [GitHub リポジトリの Issues](../../issues) からご連絡ください。
